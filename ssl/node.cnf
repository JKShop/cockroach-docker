# OpenSSL node configuration file
[ req ]
prompt=no
distinguished_name = distinguished_name
req_extensions = extensions

[ distinguished_name ]
organizationName = JKShop LLC

[ extensions ]
subjectAltName = critical,DNS:localhost,DNS:<node-domain>,IP:<IP Address>